import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
	variable: "--font-geist-sans",
	subsets: ["latin"],
});

const geistMono = Geist_Mono({
	variable: "--font-geist-mono",
	subsets: ["latin"],
});

export const metadata: Metadata = {
	title: "My Projects - Portfolio",
	description: "A showcase of my latest work and creative projects, including web applications, tools, and software solutions.",
	keywords: ["projects", "portfolio", "web development", "software", "applications"],
	authors: [{ name: "Yokesh" }],
	openGraph: {
		title: "My Projects - Portfolio",
		description: "A showcase of my latest work and creative projects",
		type: "website",
	},
};

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang="en">
			<head>
				<link rel="icon" href="/KS.png" type="image/svg+xml"></link>
			</head>
			<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>{children}</body>
		</html>
	);
}
